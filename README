Goal of the project: Command line script to export a selected directory's structure in a specific format

Things to do:

Finish XML export function
Add JSON export function
Add flags for JSON/XML format selection
Other types (delimited, visual, ect)?
